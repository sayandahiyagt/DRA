"""DRA — deep research agent bootstrap scaffold.

This package provides the project foundation: DB connectivity to Postgres with
the pgvector extension, used by the LangGraph control plane (ADR-001) as the
MVP retrieval metadata store (ADR-003).
"""

__version__ = "0.1.0"
